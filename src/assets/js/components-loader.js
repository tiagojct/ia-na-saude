/* IA · Saúde · components-loader
 *
 * Lazy-registers Web Components as they enter the viewport. Each demo is
 * defined as a custom element under src/components/*.ts. Vite bundles them
 * into _site/assets/components/<name>-<hash>.js. The manifest links names
 * to hashed paths.
 *
 * This is a stub for Phase 2 · the real registry + IntersectionObserver
 * comes in Phase 5 along with the Web Components infrastructure.
 */
(() => {
  "use strict";
  // TODO Phase 5 · wire up custom element registry + IntersectionObserver
  // hydration. For now this is a no-op so the script tag in base.njk loads
  // without 404.
})();
